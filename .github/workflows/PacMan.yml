name: Generate custom pacman maze

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "scripts/generate_pacman.py"
      - ".github/workflows/pacman.yml"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate Pacman maze SVG
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_USERNAME: ${{ github.repository_owner }}
        run: python scripts/generate_pacman.py

      - name: Commit generated SVG files
        run: |
          git config user.name "github bot"
          git config user.email "github@users.noreply.github.com"

          git add output/pacman_contribution_graph.svg output/pacman_contribution_graph_dark.svg

          if git diff --cached --quiet; then
            echo "Sem mudanças para commitar"
          else
            git commit -m "Atualiza Pacman de contribuicoes"
            git push
          fi